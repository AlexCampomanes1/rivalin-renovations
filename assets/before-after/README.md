# before-after/

This folder is intentionally empty in the local repository.

In production, before/after photo pairs live in the Azure `before-after` blob
container for this site. They are managed exclusively by the site owner via
`admin.html` — never by a developer, never by the builder deploy script.

Do not commit photos here. Do not overwrite this folder on deploy.
